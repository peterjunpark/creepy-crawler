package main

// getURLsFromHTML takes an HTML string and the root URL of the website we're crawling
// and returns an array of un[normalized] URLs found within the HTML.
//
// [normalized]: main.normalizeURL
func getURLsFromHTML(htmlBody, rawBaseURL string) ([]string, error) {

	return nil, nil
}
